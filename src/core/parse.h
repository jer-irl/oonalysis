#ifndef CORE_PARSE_H
#define CORE_PARSE_H

#include <string>
#include <vector>
#include <tuple>

void parse_files(std::tuple<std::string, std::vector<std::string>> out_and_files);

#endif // CORE_PARSE_H
