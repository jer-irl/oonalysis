#ifndef CORE_PARSE_H
#define CORE_PARSE_H

#include <string>
#include <vector>
#include <tuple>

void parse_files(const std::vector<std::string>& files);
void parse_file(const std::string& filename);

#endif // CORE_PARSE_H
